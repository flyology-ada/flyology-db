# Benchmarks

Benchmark work starts after correctness gates. Lanes remain separate: logical engine, local object service, and remote
object store. Artifacts record exact engine, adapter, compiler, dependency commit, dataset, host, topology, durability,
configuration, raw samples, and submission/visibility/remote-durable latency. No benchmark result is committed here
until its schema and provenance validator exist.
