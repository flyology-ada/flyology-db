# Flyology.DB adapter

This adapter will execute the normative NDJSON contract against the candidate engine and emit normalized outcomes,
canonical state digests, dependency provenance, and raw histories. It is implemented with the log-only slice.
